# Local input data

This directory is ignored by Git except for this file. Put local model files under `data/models` and authorized field inputs under `data/field`. The procedural demo generates its own data in memory and writes only to its selected output directory. See [data and model availability](../docs/data_and_models.md).
